/**
 * The {@code zengin} command and its exit codes.
 *
 * <p>{@link io.zengin4j.cli.Zengin#run} is the whole entry point, and it takes
 * its writers as arguments so the commands can be tested without spawning a
 * process. {@link io.zengin4j.cli.ExitCode} is a contract with the scripts that
 * call this tool.
 *
 * @since 0.3.0
 */
package io.zengin4j.cli;
