**/build/
buildSrc/build/
buildSrc/.gradle/
.gradle/
!gradle/wrapper/gradle-wrapper.jar
.idea/
*.iml
*.ipr
*.iws
.vscode/
.DS_Store
*.log
.mcp.*
*.hprof
hs_err_pid*.log
replay_pid*.log
.kotlin/
out/
*.class
.claude/
# Jazzer writes its working corpus here during fuzzing runs, and a crashing
# input here when it finds one. A finding worth keeping gets committed as a
# regression fixture rather than left in an ignored directory (ADR-0017).
.cifuzz-corpus/
.cifuzz-findings/
