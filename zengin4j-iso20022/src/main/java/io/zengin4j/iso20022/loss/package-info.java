/// The report every conversion returns alongside its output.
///
/// The vocabulary itself lives in `io.zengin4j.core.loss`: the write
/// path needs it too, and `core` cannot depend on this module (ADR-0029).
/// What is added here is a Japanese rendering and JSON.
///
/// @since 0.5.0
package io.zengin4j.iso20022.loss;
