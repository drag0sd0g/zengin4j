/// Synthetic fixtures and deterministic generators.
///
/// Published rather than test-scoped (R-M4): a project that consumes
/// zengin4j needs these on its own test class path.
///
/// **Nothing in this package is real.** Bank codes, branch
/// codes, account numbers and names are invented, and the identifiers are drawn
/// from ranges no Japanese institution uses (R-L1, P1).
///
/// @since 0.1.0
package io.zengin4j.testkit;
